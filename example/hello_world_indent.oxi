#[indent]

name := "Javan"
println("Hello,", name)

pattern is_even(x) when  x % 2 == 0
pattern is_odd(x) when x % 2 != 0


x := range(10)
each num in x:
  choose num:
      is_even -> println(num, "is even")
      is_odd -> println(num, "is odd")

